{
  "dependencies": {
    "fastify": "^3.9.1"
  },
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "build": "rimraf dist && tsc && tsc-alias"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "rimraf": "^3.0.2",
    "ts-loader": "^8.0.11",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.1.2"
  }
}
